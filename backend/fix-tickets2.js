const pool = require('./db'); pool.query("DROP TABLE IF EXISTS tickets CASCADE; CREATE TABLE tickets (id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id), route_id INTEGER REFERENCES routes(id), fare DECIMAL(10,2), status VARCHAR(50) DEFAULT 'active', qr_code VARCHAR(255), ticket_code VARCHAR(255), created_at TIMESTAMP DEFAULT NOW())").then(r => { console.log('done'); pool.end(); })
