const pool = require('./db'); pool.query("CREATE TABLE IF NOT EXISTS notifications (id SERIAL PRIMARY KEY, title VARCHAR(255), message TEXT, type VARCHAR(50) DEFAULT 'info', target_role VARCHAR(50) DEFAULT 'student', company_id INTEGER, created_by INTEGER, created_at TIMESTAMP DEFAULT NOW()); CREATE TABLE IF NOT EXISTS notification_reads (id SERIAL PRIMARY KEY, notification_id INTEGER REFERENCES notifications(id) ON DELETE CASCADE, user_id INTEGER REFERENCES users(id), read_at TIMESTAMP DEFAULT NOW())").then(r => { console.log('notifications tables OK'); pool.end(); })
